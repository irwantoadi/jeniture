<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.irwantoadi.jeniture">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".LaporanActivity" />
        <activity android:name=".KeraliyuActivity" />
        <activity android:name=".CarpenterActivity" />
        <activity android:name=".FurnitureActivity" />
        <activity android:name=".QualityActivity" />
        <activity android:name=".GelondongActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".DaftarActivity" />
        <activity android:name=".WelcomeActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>